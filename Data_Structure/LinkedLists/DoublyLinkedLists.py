# Doubly Linked Lists

# Doubly Node

class DoublyNode:
    def __init__(self, data, prev, next=None):
        self.data = data
        self.prev = prev
        self.next = next
# Creating Nodes 
head = (5)